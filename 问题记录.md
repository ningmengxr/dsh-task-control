# dsh-task-control 问题记录（待修复清单）

> 本文件记录 dsh-task-control 插件已发现但**尚未修复**的问题，供后续会话直接接手。
> 每个问题包含：现象 / 复现 / 根因分析 / 修复方向（初步）。

---

## 问题 1（2026-08-24 记录，用户要求稍后修复）

**标题**：卡在 pwsh 步骤时检测失灵 —— 反复点「拍一下」都显示"正常运行"，工具出错也检测不出来

> 注：初版记录（"提取不到报错内容"）理解有偏差，经用户澄清后重写如下。

### 现象（用户原话校准）

- pwsh **正在运行**（含卡住、但未超过卡死阈值）时，反复点「拍一下」**每次都输出"任务正在运行中，暂未出错（正在执行：pwsh）"** —— 看起来完全正常，检测不出它卡住了。
- pwsh **真正出错**时（命令执行失败/返回错误），**也检测不出来**，不会显示"任务出错"。

### 检测逻辑现状（src/client/buttons.tsx L134-161 + settings.ts 默认文案）

优先级：`lastError` > `stuck`（runningCalls 超 10 分钟）> 运行中 > 正常
默认文案：healthyText=`任务正常，无异常` / errorText=`任务出错：{error}` / runningText=`任务正在运行中，暂未出错`

### 根因分析

1. **卡死阈值过长（10 分钟，STUCK_THRESHOLD_MS）**：pwsh 卡住未超阈值时，`stuck` 判断不成立 → 落入 `runningCalls.length > 0` 分支 → 显示 runningText"任务正在运行中，暂未出错"。对 pwsh 这类通用工具，卡 1~2 分钟就该提示，10 分钟用户根本无法提前感知（下载类靠 isDownloadCall 特殊处理，pwsh 没有）。
2. **`lastAgentError` 不覆盖工具调用错误**：`lastError` 只在 **agent 层**报错时非空（如模型 API 失败）；工具（pwsh）执行失败返回的错误结果**不会**映射到 lastAgentError → 出错分支永远走不到 → pwsh 命令失败时显示"运行中"甚至"任务正常，无异常"（若 running 已 false）。
3. **检测只读 snapshot 的 running/lastAgentError/runningCalls 三个字段**：工具错误结果实际存在于会话节点（nodes）里，客户端检测逻辑没读取 → 无法识别"工具已返回错误"。

### 影响

- 用户在 pwsh 卡死/出错时得到**虚假的"正常"反馈**，会一直干等；等超过 10 分钟才提示"疑似卡死"，此时才出现强制终止入口。
- 与插件"检测"的核心价值（及时发现异常）相悖。

### 修复方向（初步想法，待细化）

1. **阈值按工具类型区分**：下载/安装类保持长阈值（10 分钟），pwsh 等通用工具用短阈值（如 3 分钟，可进设置卡）；或对 pwsh 直接识别静默挂起特征。
2. **读取工具错误结果**：从 `snapshot.nodes` 找最近一次 tool 调用结果，若含 error/失败标志 → 走"任务出错"分支并显示工具错误内容（即使 running 仍为 true）。
3. **pwsh 专项提示**：卡住工具名为 pwsh 时，提示"pwsh 卡住常见原因是子进程句柄继承（cmd 后台进程未退出），通常只能强制终止"。
4. **文案区分**：runningCalls 非空时明确显示"正在执行：X（已运行 N 秒）"，让用户看到时长而不是笼统的"暂未出错"。

### 状态

✅ **已修复（2026-08-24）** —— 按"下载方向"实现 pwsh 状态实时检测：

**修复内容**
1. **宿主新增路由 `/dsh-task-control/pwsh-status`**（src/index.ts `queryPwshStatus`）：
   - 按 marker 匹配 pwsh/powershell 进程（无 marker 时取最新启动的兜底）
   - 性能计数器 `Win32_PerfFormattedData_PerfProc_Process` 按 PID 查**瞬时 IO 速率**（查两次预热，300ms 间隔）
   - 输出文件大小（-o/-OutFile 提取）+ URL HEAD 总量（可算百分比）
   - 输出全数字（`count|io|size|total`），规避 PowerShell 输出编码问题
2. **客户端（buttons.tsx）**：pwsh 调用（name 含 pwsh 且非下载类）**点击即查宿主**，不再干等 10 分钟阈值。检测结果四档：
   - 有总量 → 显示百分比（同下载）
   - 有 IO 速率/文件增速 → 显示"当前 IO 速率 x KB/s"或"输出速度 x KB/s"（文件增速用两次采样差值）
   - IO 为 0 且无文件 → "pwsh 进程无任何输出活动，疑似卡住，可强制终止"
   - 进程已退出 → "进程已退出（可能异常中断），可强制终止"（补上 lastAgentError 不覆盖工具错误的盲区）
   - 卡死分支已排除 pwsh（pwsh 走专门查询）；非 pwsh 工具仍保留 10 分钟阈值兜底
3. **构建产物**：lib/index.js 15.4kb / lib/client.js 35.9kb

**验证结果（3090 启动安全测试 + 实测）**
- 路由挂载：GET → 405 ✓
- 卡住进程（`Start-Sleep`，无 IO）：active=True, io=0 → 客户端显示"疑似卡住" ✓
- IO 活动进程（持续写文件）：active=True, io>0（25~26 B/s），文件 346→458 字节增长 ✓
- 不存在 marker：active=False → 客户端显示"进程已退出" ✓
- 测试陷阱记录：marker 字面量若出现在测试命令自身命令行会被误匹配（用 GUID 表达式/从文件读 marker 规避）；Stop-Process 清理要 `-ne $PID` 防自杀

**待办**：正式 DSH 需重启后宿主路由生效（lib/index.js 经 junction 已更新）；用户浏览器硬刷新加载新 bundle。

---

## 问题 2（2026-08-24 记录，**已修复**）

**标题**：新版 DSH 升级后，插件加载失败崩溃（`settings.plugin.item` 变成严格 keyed slot，要求 `options.key`）

### 现象（用户浏览器截图）
DSH 启动显示 "Failed to load plugins"，dsh-task-control 报：
```
failed to apply loader entry 163a3839 (dsh-task-control): keyed slot
"settings.plugin.item" requires options.key
```
DSH fail-loud 崩溃无法启动（用户用急救工具箱全禁了插件才恢复）。

### 根因
新版 DSH 把 `settings.plugin.item` 从宽松 list/可选 key 改成了**严格 keyed slot**（`ui-slots` 校验 `register` 必须带 `options.key`，即插件的 settings 命名空间）。我们插件客户端注册时只给了 `id` 没给 `key` → apply 抛错 → 整插件加载失败。

### 修复（已构建 + 3090 验证通过）
1. **客户端** `src/client/index.ts`：settings.plugin.item 注册加 `key: 'dsh-task-control'`。
2. **宿主** `src/index.ts`：新增 `registerSettingsNamespace`，动态 import `@deepseek-ai/dsh-settings`（新版 DSH 已放进 `~/.dsh/profiles/node_modules`）的 `installSettingsSection` 注册同名 namespace → host serve 该命名空间，设置卡才被 dispatch 显示；整体 try-catch，失败只导致设置卡不显示、不影响核心功能（按钮/急停/检测）。
3. **关键新认知**：新版 DSH 把大量 `@deepseek-ai/*` 包放入 `~/.dsh/profiles/node_modules`（dsh-settings/schemastery 等都在），外部宿主插件**现在可以 import/动态加载**它们——推翻旧规则"宿主不得 import @deepseek-ai"（旧规则基于当时的模块解析失败）。
4. 验证：3090 启动安全测试 HTTP 200 就绪 + 5 条宿主路由（resume/pwsh-status/kill/download-status/pause）全部 405（apply 完整执行、slot 注册带 key 成功）。

### 遗留
- 发布 v0.4.1（或下版）需包含此修复（lib/index.js + lib/client.js 已更新）。
- ui-aqua、dsh-hook 也在 failed 列表——ui-aqua 若有类似 key 问题需单独检查（官方插件多半已适配）；dsh-hook 是宿主插件不注册设置卡，可能无 key 问题。用户可用工具箱逐个启用以排查。

### 状态
✅ 已修复（2026-08-24）；待发布到 npm/执行更新
